# JOGO-MON Backend — pinned deps (Python 3.11)
# Install: python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==13.1

# --- Data layer ---
sqlalchemy==2.0.36
asyncpg==0.30.0
greenlet==3.1.1

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Auth & crypto ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
# bcrypt dipatok <4.1: passlib 1.7.4 belum kenal __about__ di bcrypt 4.1+
bcrypt==4.0.1
cryptography==44.0.0

# --- Cache ---
redis==5.2.1

# --- Collector (SSH + SNMP) ---
paramiko==3.5.0
pysnmp==6.2.6

# --- Outbound notif (Telegram) ---
httpx==0.28.1

# --- Migrations ---
alembic==1.14.1

# --- Test ---
pytest==8.3.4
pytest-asyncio==0.25.0
